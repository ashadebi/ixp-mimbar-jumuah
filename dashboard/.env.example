# Load explicitly through your service manager; never commit real keys.
MIMBAR_VALIDATOR=disabled
MIMBAR_BIRD_IMAGE=mimbar-bird:2.19.2
MIMBAR_ALLOW_DEPLOY=0
MIMBAR_SSH_KEY=
MIMBAR_KNOWN_HOSTS=
# Set to 1 when accessed through an HTTPS reverse proxy.
MIMBAR_SECURE_COOKIE=0
